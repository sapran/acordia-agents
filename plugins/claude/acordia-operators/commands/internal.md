---
description: Internal network and Active Directory — AD attacks, Kerberos abuse, credential access, lateral movement.
argument-hint: '[domain, subnet, or host in scope]'
# Short alias for `internal-network`; that wrapper is the canonical one.
---

Dispatch the `internal-network` agent (ACORDIA Operations) with the brief below.

Use it for directory enumeration, ticket-based credential access, and lateral movement across Windows, Linux, and internal services.

**Brief:** $ARGUMENTS

If the brief is empty, ask what to look at before dispatching — this agent reads and judges, so it needs a concrete artefact, host, or question. Report back what it returns without re-deriving it.
