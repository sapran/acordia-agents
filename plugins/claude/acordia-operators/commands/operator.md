---
description: Hand an authorized engagement to the orchestrating offensive-security brain.
argument-hint: '[scope and objective of the engagement]'
---

Hand the work below to the `operator` agent (ACORDIA Operations), which orchestrates this pillar.

It holds the engagement roster, routes recon-through-exploitation phases to its four domain specialists, and composes the final report.

**Brief:** $ARGUMENTS

Dispatch it as a subagent if this harness allows dispatching a primary agent; otherwise switch the session to `operator` and carry the brief across. If the brief is empty, ask what the operation needs before starting — do not invent scope.
