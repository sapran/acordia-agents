---
description: Web application and API security testing — OWASP WSTG methodology.
argument-hint: '[target URL or API surface]'
# Short alias for `web-application`; that wrapper is the canonical one.
---

Dispatch the `web-application` agent (ACORDIA Operations) with the brief below.

Use it for authentication/authorization, injection, business logic, and API-specific testing.

**Brief:** $ARGUMENTS

If the brief is empty, ask what to look at before dispatching — this agent reads and judges, so it needs a concrete artefact, host, or question. Report back what it returns without re-deriving it.
