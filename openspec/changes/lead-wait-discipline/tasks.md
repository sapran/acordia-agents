## 1. Source of truth

- [x] 1.1 Add the lead-side interstitial-work competency to the Core-column prose in `docs/roles/operational-analyst.md`, leaving the grid table untouched, and verify by `git diff --stat` showing the table rows unchanged and by re-running the grid-transcription check in `CLAUDE.md` to `problems: 0` with the full row count

## 2. Free the characters — relocate a duplicated enumeration

- [x] 2.1 Add the canonical credential brief packet defined in `proposal.md` to `acordia-analysts/skills/credential-harvest-triage/SKILL.md`: scoped collections, corpus state, asset/system classes, non-secret fingerprints, evidence and provenance, observed/inferred status, freshness, confidence, mission relevance, expected credential forms, planned specialist owners, named mission/value gaps, exposure, omissions. **Measured: 14 fields, and every obligation of all four prior lists is preserved.** Field-level coverage is complete for all four: the skill's own prior list required the packet to contain `the asset register`, and **the asset register is field 3** (`asset/system classes`), which is also the artefact triage step 7 extends. What travels *alongside* the packet is the distinct **asset-and-credential hypothesis register** the lead fuses, per the MODIFIED requirement's own text — two different registers, two different homes, as recorded in `proposal.md`
- [x] 2.2 Verify the skill's `metadata.acordia` block is unchanged (`grid_row: null`, `procedural: true`) and that `~/ai/checks/check-acordia.sh .worktrees/lead-wait-discipline` still reports 42 grid rows against 42 anchored skills
- [x] 2.3 Remove both enumerations from `acordia-analysts/agents/cyber-analyst.md`, leaving the surrounding routing sentence that names `credential-harvest-triage` intact, and verify the gate body metric — `len(f.read_text().split('---\n',2)[2].strip())` — dropped from 10,391. **Measured: 10,391 → 10,125, freeing 266 and leaving a 375-character budget** (less than the 432 forecast, because the lists were replaced by routing sentences rather than deleted outright)
- [x] 2.4 Add a bounded "while legs are still out" subsection to `acordia-analysts/skills/maintaining-operating-picture/SKILL.md` `## Method`, covering what the lead establishes without a pending return — verifying returns already in hand, sizing the corpus, naming the gaps the dispatched legs are not covering — and verify the existing five method bullets on freshness, deltas and decay are unchanged. Add **no** `doctrine_source`: `doctrinal-provenance` requires a Method procedure to trace to its grid row and carry no literature attribution. Verify the `metadata.acordia` block is byte-unchanged. No ceiling applies to skill bodies

## 3. Orchestrator doctrine — three surfaces, byte-identical

- [x] 3.1 In `acordia-analysts/agents/cyber-analyst.md`, add the waiting-is-residual clause naming `maintaining-operating-picture`, the join-before-exit rule with its record-in-the-product obligation, and the positive "check every assignment names its agent" property. **371 characters used against the measured 375 budget.** All five spec scenarios map to a sentence: waiting as the residual case plus the skill name, leg-running blocks session end, cancelled loss recorded in the product, the agent-naming check, and byte-identity across the three surfaces via gate check 7
- [x] 3.2 Apply the identical edit to `acordia-analysts/commands/cyber-analyst.md` and `acordia-analysts/commands/analyst.md` by exact body substitution, and verify `~/ai/checks/check-acordia.sh` check 7 reports the orchestrator body intact across all three artifacts
- [x] 3.3 Verify `check-acordia.sh` check 8 passes. **Final measured body 10,496 of 10,500 — 4 characters of margin**, after the reviewer's prose fixes and a deliberate trim ("the analysis needing" → "analysis needing") to avoid landing at the ceiling exactly. No second relocation was needed. The margin is thin: any future addition must relocate something first

## 4. Version and gates

- [x] 4.1 Bump `6.19.1` → `6.20.0` in all three files (`acordia-analysts/.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, `.omp-plugin/marketplace.json`) and verify `check-acordia.sh` reports one semver at exactly three occurrences and byte-identical catalogs — **ok version 6.20.0 x3 across 3 files**
- [x] 4.2 Run `openspec validate --all --strict` and verify it passes with no omitted-scenario error on `agent-roster` — **7 passed, 0 failed**
- [x] 4.3 Run the skill-sets transcription check from `CLAUDE.md` and verify `problems: 0`, since a prompt was edited — **problems: 0**

## 5. Review and land

- [x] 5.1 Confirm nothing leaked into the main checkout: `git -C ~/git/acordia-agents status --porcelain` shows no changes to `acordia-analysts/` or `docs/` — **CLEAN**
- [x] 5.2 Dispatch `reviewer` and `security-reviewer` on the diff, and verify each finding is fixed or dismissed in writing. **`reviewer` returned 10 findings; 9 fixed, 1 dismissed with reason.** Fixed: the residual fourth packet enumeration at `SKILL.md:61` reduced to a pointer; the asset register given an explicit home in field 3 so triage step 7 has a source; task 2.1's four-way superset claim corrected to name the relocated register; the `maintaining-operating-picture` description widened so the skill is reachable by description match, plus a lead-only scope guard since `Coll` also carries it; "Waiting is the residue, not the start" → "Wait only when none is left"; "Check every assignment names its agent", with "general-purpose agent" replacing a third vocabulary for one referent; "what that cost" → "what that gave up"; proposal citations reduced to register keys; the hard-coded "four legs" dropped. **Dismissed:** the P1 reattributing "directs each step" from `ACORDIA` p. 21 to `Monte` p. 60 — the primary at `1152d85c` p. 21 carries that wording verbatim, footnoting Monte at its note 48; the reviewer relied on a derivative summary in `docs/methodology-alignment-proposal.md`; and `Monte` p. 60 sits outside the grounding set selected for this change. The disputed phrase was removed from the new prose anyway, so it now quotes only undisputed `ACORDIA` pp. 21 and 23. **`security-reviewer` was UNAVAILABLE — it refused and had no bash tool, so there is no independent security verdict on this change.** As a manual fallback I ran its three checks myself and found no guardrail, read-only-evidence, retrieved-content or credential restriction removed from any of the three surfaces, no credential values in the new table, and brief framing intact per gate check 7. Self-review is not a substitute for the reviewer; task 6.1 re-runs review against the final diff
- [x] 5.3 Install the built version into a scratch omp profile and verify `/agents` lists all five agents and the lead dispatches one leg — the distribution's own "it loads and runs" gate. **Run in throwaway profile `test-profile` (did not pre-exist).** Dry run reported 5 agents + 46 skills; install linked them and the served copy carried the new doctrine (`Check every assignment names its agent` present in the linked `cyber-analyst.md`; `While dispatched legs are still out` present in the linked `maintaining-operating-picture/SKILL.md`), proving the worktree tree was what loaded rather than a stale copy. `omp --profile test-profile -p` listed all five ACORDIA agents. A real dispatch through the `task` tool to `terrain-analyst` returned `ACORDIA-LEG-OK`, so a leg loads and runs. **Cleanup: `tools/uninstall-omp.sh --profile test-profile` removed 51 links; a `find -type l -lname "*acordia-agents*"` sweep confirms zero ACORDIA symlinks remain under that profile, so the test install cannot shadow a later plugin check**
- [x] 5.4 Commit the implementation, then re-run `~/ai/checks/check-acordia.sh` so check 4 sees a **non-zero** artifact and version-file diff against `develop`. **Committed as `5c096eb`; check 4 now reports `artifacts=5, version files changed=3 vs develop`**, so artifact/version coupling is proven rather than vacuously passing. Throwaway profile `/Users/user/.omp/profiles/test-profile` removed in full after verification and confirmed absent; no other profile touched

## 6. Repository workflow — after apply, before merge

These are the `acordia-agents-change` landing steps, not apply steps. They are listed so nothing is
stranded, and are deliberately left unchecked while `/opsx:apply` is the active action.

- [x] 6.1 Re-run the reviewer against the **final** diff. **Done against commit `5c096eb`; 4 findings, all resolved.** (a) **Blocker, no gate could see it:** the widened `maintaining-operating-picture` description was 253 characters against `skill-library`'s normative 1–200 limit; the drift gate measures no description and `openspec validate` checks spec documents rather than the tree against them. Trimmed to 190 — library max is now 196 (`vuln-attacksurface-mapping`, pre-existing) and mean 174.35, both within the ceiling scenario, so `skill-library` genuinely needs no delta and the proposal's claim holds. (b) **Cross-rule conflict:** the retained line-68 rule "Wait only when a next question needs a return" conjoined with the new "Wait only when it is exhausted" left no licensed way to wait for a leg nothing downstream needs, making cancel-and-record the only compliant ending — the exact disposal this change exists to stop. Fixed by `take its return` → `wait for its return`, licensing the join as a wait. (c) Pronoun with no antecedent: `Wait only when none is left` → `Wait only when it is exhausted`, reusing the antecedent from "carries it". (d) Same defect in the skill's scope guard: "a leg has none of its own" → "a leg dispatches no legs of its own". Funded by `with a leg running` → `with a leg out`, which also matches "While legs are out" in the same paragraph. **Final body 10,496, margin 4**; reviewer's verdict with the blocker resolved: safe to archive and merge
- [ ] 6.2 `openspec archive lead-wait-discipline --yes`, re-run `openspec validate --all --strict`, and commit the archive in the same PR as the prose — the repo workflow requires the archive to travel in the feature PR

## 7. Effect measurement

- [x] 7.1 Record in `docs/implementation-notes.md` that the 21.2-hour waiting figure motivated this change but does not measure it, and that crediting the doctrine with reduced lead idling needs a fresh prospective A/B scored from session JSONL rather than a re-read of the motivating corpus — **done, under `## Parked during lead-wait-discipline`, together with the 7-character margin warning, the `maxConcurrency` evidence, the `skill://` resolver gap and the aleph-mcp field report**
