# Tasks

## 1. Rename the alias
- [x] 1.1 `git mv` `commands/defender.md` to `commands/overwatch.md`
- [x] 1.2 Confirm the wrapper still names `overwatch-analyst` and keeps description + argument-hint
- [x] 1.3 Confirm the count stays 18, nine canonical and nine aliases

## 2. Update the three handle sites
- [x] 2.1 Short-handle list in `README.md`
- [x] 2.2 Provenance note in `docs/roles/operational-analyst.md`
- [x] 2.3 Confirm the adversary sense of `defender` is untouched everywhere else

## 3. Fix the requirement
- [x] 3.1 Replace the leg-alias-retention paragraph with the derive-from-agent rule
- [x] 3.2 Replace the alias scenario with one asserting every alias derives from its agent
- [x] 3.3 `openspec validate --all --strict`
