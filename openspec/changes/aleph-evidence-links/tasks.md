## 1. Reporting contract and Aleph procedure

- [x] 1.1 Update the source-of-truth reporting paragraph and `briefing-reporting` so safe known evidence-store locators are navigable, with the explicit unavailable-link exception; verify source prose and shared skill agree.
- [x] 1.2 Add `aleph-entity-graph` guidance and its named co-located reference for the canonical entity tuple, authorised UI-origin inputs, `/entities/<id>` route, and receipt-driven rendering; verify every reference pointer is resolvable.

## 2. Rendered-report coverage verification

- [x] 2.1 Add a verdict-only receipt-to-HTML coverage check to the Aleph reference and exercise a clean fixture plus missing-link, wrong-origin, unsafe-link, href-less-anchor, and secret-output mutation cases.
- [ ] 2.2 Align the credential-sweep HTML layout and its self-check with the shared Aleph receipt and coverage semantics; verify existing safe-link and disclosure checks remain represented.

## 3. Distribution and specification

- [x] 3.1 Add the observable Aleph link and coverage contract to the `skill-library` delta spec; validate the delta’s scenario coverage against the published specification.
- [ ] 3.2 Bump all three version declarations and regenerate `acordia-map.html` from the final authored tree; verify the map’s cold landing page and report-skill route.

## 4. Change verification and completion

- [ ] 4.1 Run the rendered-report mutation proof, `~/ai/checks/check-acordia.sh`, and `openspec validate --all --strict`; record exact passing evidence.
- [ ] 4.2 Review the worktree diff for correctness and safety, resolve every finding, archive the approved change, and re-run the OpenSpec and repository gates.