---
description: What is the estate made of, where can we move, what does the trust allow — and did the system change?
argument-hint: '[host, network, identity, cloud or application terrain to read]'
# Short alias for `terrain-analyst`; that wrapper is the canonical one.
---

Dispatch the `terrain-analyst` agent (ACORDIA Analysis) with the brief below.

Use it for network, identity, cloud, application and host terrain analysis, attack-surface mapping, and effect-on-system verification.

**Brief:** $ARGUMENTS

If the brief is empty, ask what to look at before dispatching — this agent reads and judges, so it needs a concrete artefact, host, or question. Report back what it returns without re-deriving it.
