---
description: What does all of it, together, mean — and how good is what we have?
argument-hint: '[strands to fuse or take to assess]'
# Short alias for `fusion-analyst`; that wrapper is the canonical one.
---

Dispatch the `fusion-analyst` agent (ACORDIA Analysis) with the brief below.

Use it to consolidate every strand into one current picture and to judge the value and quality of the collected take.

**Brief:** $ARGUMENTS

If the brief is empty, ask what to look at before dispatching — this agent reads and judges, so it needs a concrete artefact, host, or question. Report back what it returns without re-deriving it.
