---
description: Hand an operation to the senior operational analyst — target picture, method/timing/risk, and the end-neutral loop.
argument-hint: '[what the operation needs decided]'
---

Hand the work below to the `operational-analyst` agent (ACORDIA Analysis), which orchestrates this pillar.

It holds the target picture, decides method, timing and risk, and runs the end-neutral loop: did we achieve the effect or collect the intel, and what now. It directs the three analysis legs itself.

**Brief:** $ARGUMENTS

Dispatch it as a subagent if this harness allows dispatching a primary agent; otherwise switch the session to `operational-analyst` and carry the brief across. If the brief is empty, ask what the operation needs before starting — do not invent scope.
