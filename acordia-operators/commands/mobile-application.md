---
description: Mobile application security testing against OWASP MASTG/MASVS.
argument-hint: '[app package or IPA/APK path]'
---

Dispatch the `mobile-application` agent (ACORDIA Operations) with the brief below.

Use it for static and dynamic analysis, storage, crypto, auth, IPC, and resilience-bypass coverage.

**Brief:** $ARGUMENTS

If the brief is empty, ask what to look at before dispatching — this agent reads and judges, so it needs a concrete artefact, host, or question. Report back what it returns without re-deriving it.
